import { createSlice } from "@reduxjs/toolkit";

const initialState = {
    workerData:null
}

const workerSlice = createSlice(
    {
        name:"Worker",
        initialState,
        reducers:{
            
        }
    }
)